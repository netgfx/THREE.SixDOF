/**
 * A small wrapper for THREE imports so rollup tree-shakes only the parts we need better
 */
export { Object3D, ShaderMaterial, BackSide, Mesh, Points, SphereGeometry, Texture, NearestFilter, LinearFilter, RGBAFormat, SRGBColorSpace, } from 'three';
